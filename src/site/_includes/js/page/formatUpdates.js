// Header JS
  // toggle menu
  // initial "header.dataset.state == 'closed'"
  var toggleSections = document.querySelectorAll(`[data-show-states]`);
  
  toggleSections.forEach(setSections = () => {
    
  });